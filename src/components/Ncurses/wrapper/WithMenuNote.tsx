import React from 'react';

//type MenuNoteProps = {
//	content: string
//}
const MenuNote: React.FunctionComponent = ({children}) => (
  <div className="note">{children}</div>
);

export default MenuNote;
